#pragma once

#include "common.h"
#include "abstractbutton.h"

typedef struct QtC_PushButton QtC_PushButton;

#define PUSHBUTTON_DECLARE(Self) \
    ABSTRACTBUTTON_DECLARE(Self)

#define PUSHBUTTON_DEFINE(Self) \
    ABSTRACTBUTTON_DEFINE(Self)

