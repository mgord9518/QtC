WIP C bindings to Qt

Once I think they've gotten to a decently usable point, the Zig bindings will
be moved to their own repo

Building:
  mkdir build
  cd build
  cmake ..
  cd ..
  cmake --build build

  zig build
